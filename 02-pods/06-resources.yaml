apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
spec:
  containers:
  - name: test
    image: nginx
    resources:
      requests:
        cpu: "500m"
        memory: "64Mi"
      limits:
        cpu: "800m"
        memory: "128Mi"